{
  "name": "wbc-demo",
  "version": "0.0.0",
  "private": true,
  "description": "Interactive in-browser demo for wbc-mjlab WBC policies (Three.js + mujoco-wasm + onnxruntime-web).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "validate": "tsx scripts/validate.ts"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^5.2.0",
    "three": "^0.185.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "@types/three": "^0.185.0",
    "tsx": "^4.20.6",
    "typescript": "^6.0.3",
    "vite": "^8.1.0"
  }
}
